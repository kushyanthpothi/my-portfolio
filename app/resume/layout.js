export const metadata = {
    title: 'My Resume',
    description: 'Professional Resume',
};

export default function ResumeLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    );
}
